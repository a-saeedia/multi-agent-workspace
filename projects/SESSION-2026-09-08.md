# Session 2026-09-08: Multi-Project Init

User's key constraints voiced throughout the conversation:
- **Zero budget** — everything must be free / open-source.
- **Agent-driven** — user wants agents to do the heavy lifting (Hermes, OpenCode),
  not manual GUI work.
- **No VPS desire** — wants serverless/lightweight hosting.

## Key decisions
1. **Telegram bot → Cloudflare Workers.** Clarified the "runs on Telegram
   directly" claim is a false narrative — Telegram still needs a backend; the
   change is VPS → serverless function. CF Workers chosen.
2. **Video editing → dropped Adobe Premiere.** Premiere UXP isn't
   agent-friendly. Chose FFmpeg + Python + Hermes (orchestrator) + OpenCode
   (inference). The user has a `premiere-prpr` bridge project in the vault —
   worth cross-checking before abandoning Premiere entirely.
3. **City animation → Blender stays.** User already uses Blender and will
   delegate modeling/animation to agents. Auto-Rig Pro for characters,
   OpenCode for dialogue, Hermes for scene orchestration.
4. **Multi-agent orchestration layer** — Claude-Code-style parallel agents via
   Python controller: task queue → dispatch → parallel workers → aggregate.
   "FreeBuf" in transcript = free/open scaling (NOT the security site).

## Deliverables
- `C:\Users\user\Desktop\multi\projects\` — 4 project folders + master index.
- Mirrored into vault `projects/`.
- Knowledge graph entities + relations created.

## Suggested build order
04 (orchestration foundation) → 01 (quick win) → 02 (uses orchestration) → 03 (marathon).